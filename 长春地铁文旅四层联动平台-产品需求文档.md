# 长春地铁文旅四层联动平台

## 产品需求文档

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 长春地铁文旅四层联动平台 |
| 文档版本 | V1.0 |
| 文档状态 | 需求评审稿 |
| 文档日期 | 2026-09-04 |
| 产品形态 | Web 端沉浸式地图 / 城市文旅信息平台 |
| 核心技术方向 | Three.js + WebGL + 2D 地图图层 |
| 首期目标 | 完成四层空间叙事和联动交互的可用原型，验证用户是否能顺畅发现“区域 - 地铁 - 景点”的关系 |

---

## 1. 项目背景

长春的城市分区、地铁线路和文旅景点目前容易被用户分别理解：

- 用户知道某个景点，但不知道它属于哪个区域、如何通过地铁到达。
- 用户知道某条地铁线，但不知道沿线有哪些值得游览的景点。
- 用户看到普通地图时，难以形成对城市空间关系、地铁走向和景点分布的整体认知。
- 传统旅游介绍以列表和图文为主，缺少“沿着地铁探索城市”的连续体验。

本产品通过四个互相关联的空间层级，将长春城市信息组织成一条递进式叙事：

1. **城市总览层**：从立体城市和行政 / 功能分区认识长春。
2. **地铁空间层**：进入地铁视角，理解线路、站点、方向和上下关系。
3. **景点内容层**：以 2D 方式浏览景点分布，并进入视频、全景照片和详细介绍。
4. **2D 关系总览层**：用易读的平面地图同时查看分区、地铁和景点。

四层不是四个独立入口，而是围绕同一对象进行逐级聚焦：

> 城市区域 → 区域内地铁 → 地铁沿线景点 → 2D 全局关系复盘

---

## 2. 产品定位

### 2.1 一句话定位

一个以 Three.js 为核心、通过四层空间视角讲述“长春分区 - 地铁 - 景点”关系的沉浸式城市文旅 Web。

### 2.2 核心价值

- **对游客**：快速发现“坐哪条线、在哪站下、周边有什么”。
- **对本地居民**：用新的空间方式重新认识城市和地铁沿线。
- **对文旅传播**：将景点内容从静态列表升级为可探索的城市故事。
- **对运营方**：沉淀分区、站点、线路、景点和内容之间的结构化数据。

### 2.3 产品原则

1. **先理解空间，再查看内容**：先知道对象在哪里，再进入详细介绍。
2. **一份数据，多种表达**：区域、地铁站、景点使用统一 ID，在四层中保持一致。
3. **连续探索，不丢上下文**：用户切换层级时，保留当前选中的区域、线路、站点或景点。
4. **体验优先于建模精度**：首期重点验证空间叙事和交互，不把真实城市级精细建模作为前置条件。
5. **动效服务理解**：层级切换、镜头移动和高亮用于表达关系，不做无意义炫技。

---

## 3. 用户与使用场景

### 3.1 目标用户

| 用户 | 特征 | 核心需求 |
| --- | --- | --- |
| 外地游客 | 对长春不熟悉，出行时间有限 | 快速规划地铁沿线游览 |
| 城市探索者 | 喜欢通过地图、路线和故事发现城市 | 发现陌生区域和隐藏景点 |
| 学生 / 年轻用户 | 对交互和视觉体验敏感 | 获得可分享、可沉浸的城市体验 |
| 文旅工作人员 | 需要展示城市文旅资源 | 以统一平台对外介绍区域和景点 |
| 内容运营人员 | 负责景点资料和媒体内容 | 维护景点、线路和专题内容 |

### 3.2 典型任务

**任务 A：从区域开始探索**

1. 用户进入平台看到长春城市 3D 总览。
2. 点击一个区域，区域高亮并显示区域摘要。
3. 进入该区域的地铁视角，查看经过的线路和站点。
4. 选择一个站点，查看附近景点。
5. 点击景点，进入景点详情，观看视频或全景照片。

**任务 B：从地铁线开始规划**

1. 用户在地铁层选择一条线路。
2. 系统突出线路走向、站点顺序和方向。
3. 用户点击站点，查看站点周边景点。
4. 用户切换到 2D 总览，查看线路、分区与景点的全局关系。

**任务 C：从景点开始了解城市**

1. 用户在景点层浏览景点贴图。
2. 点击景点查看详情和媒体内容。
3. 查看“所属区域”“最近地铁站”“推荐线路”。
4. 返回对应的地铁层或区域层继续探索。

---

## 4. 产品信息架构

### 4.1 页面结构

```text
首页 / 四层体验入口
├── 第一层：城市 3D 总览
│   ├── 分区选择
│   ├── 城市整体状态
│   └── 进入对应区域
├── 第二层：地铁空间层
│   ├── 线路选择
│   ├── 站点选择
│   ├── 上下 / 方向 / 换乘表达
│   └── 进入沿线景点
├── 第三层：景点内容层
│   ├── 2D 景点贴图
│   ├── 景点筛选
│   ├── 景点详情
│   └── 视频 / 全景 / 图文内容
└── 第四层：2D 关系总览
    ├── 分区
    ├── 地铁线路
    ├── 站点
    ├── 景点
    └── 图层筛选与回到具体对象
```

### 4.2 全局导航

页面始终提供：

- 四层进度导航：`城市`、`地铁`、`景点`、`总览`。
- 当前层级名称。
- 面包屑：`长春 > 区域 > 线路 > 站点 > 景点`。
- 返回上一级。
- 全局搜索。
- 图层 / 筛选入口。
- 帮助与图例入口。

导航应表现为“进入下一种观察方式”，而不是传统多页跳转。切层时保留选中对象，并通过镜头、图层透明度和高亮动画建立连续感。

---

## 5. 核心功能需求

## 5.1 第一层：城市 3D 总览层

### 5.1.1 目标

让用户建立对长春城市分区和空间关系的第一印象，并选择接下来要探索的区域。

### 5.1.2 页面内容

- 3D 城市示意模型。
- 分区边界或分区面片。
- 区域名称标签。
- 重要地标 / 城市节点的简化体块。
- 当前视角控制。
- 区域筛选和搜索。
- 当前区域摘要面板。
- “进入地铁层”操作。

### 5.1.3 交互规则

| 操作 | 反馈 |
| --- | --- |
| 鼠标拖拽 / 触摸拖动 | 旋转城市视角 |
| 滚轮 / 双指缩放 | 缩放视图 |
| 点击区域 | 区域高亮、镜头聚焦、显示摘要 |
| 点击空白处 | 取消区域选择 |
| 点击进入地铁层 | 带着当前区域进入第二层 |
| 点击地标 | 显示地标名称，可跳转至相关景点内容 |

### 5.1.4 区域摘要

区域摘要至少包含：

- 区域名称。
- 区域定位或一句话介绍。
- 地铁线路数量。
- 地铁站点数量。
- 景点数量。
- 推荐探索入口。

### 5.1.5 首期建模策略

首期不要求完成真实城市级精细建模，使用以下可替换资产：

- 分区：带高度的简单多边形或挤出面。
- 建筑：规则体块、低模建筑或聚合建筑体。
- 道路：简化线段或贴图。
- 水系 / 公园：平面色块或低模平面。
- 地标：少量具有识别度的占位模型。

模型数据和渲染逻辑必须分离。后续替换为真实 GIS、倾斜摄影、GLB / GLTF 或 3D Tiles 时，不应改变用户交互和内容数据结构。

---

## 5.2 第二层：地铁空间层

### 5.2.1 目标

以地铁为主要叙事对象，让用户理解线路走向、站点顺序、方向、换乘关系及地铁与地面景点的关系。

### 5.2.2 页面内容

- 当前区域对应的地铁线路。
- 线路走向和站点顺序。
- 站点节点。
- 上行 / 下行或方向信息。
- 换乘站表达。
- 地面与地下的空间关系。
- 线路筛选。
- 站点详情。
- 沿线景点入口。

### 5.2.3 视觉表达

- 地面区域保持低饱和、半透明或弱化处理。
- 地铁线路使用具有区分度的颜色。
- 地铁线采用可读的空间高度，避免与地面区域重叠。
- 地铁站点用稳定尺寸的节点表达，缩放时不发生不可控变形。
- 换乘站使用复合节点或特殊图标表达。
- 上下关系使用高度、垂直连接线或剖切视图表达。
- 当前线路和当前站点始终保持高亮。

### 5.2.4 交互规则

| 操作 | 反馈 |
| --- | --- |
| 点击线路 | 线路高亮，其他线路弱化，显示线路信息 |
| 点击站点 | 镜头聚焦，显示站点名称、方向和附近景点 |
| 点击线路端点 / 方向 | 显示方向和站点顺序 |
| 点击换乘站 | 显示可换乘线路 |
| 点击沿线景点 | 进入第三层并保留当前站点 |
| 切换 2D / 3D | 保留线路、站点和视野中心 |

### 5.2.5 站点信息

- 站点名称。
- 所属线路。
- 站点序号。
- 上一站 / 下一站。
- 运行方向。
- 换乘线路。
- 周边景点。
- 建议游览时长。
- 到景点的步行距离或出站方向。

注：运营线路、站点、票价、首末班车等动态信息应由运营后台或权威数据源维护，不能写死在前端。

---

## 5.3 第三层：旅游景点 2D 内容层

### 5.3.1 目标

将地铁沿线和城市区域中的文旅资源转化为可浏览、可阅读、可观看的内容对象。

### 5.3.2 页面内容

- 2D 地图底图。
- 景点贴图 / 图标 / 缩略卡片。
- 景点类别筛选。
- 与当前区域、线路、站点关联的景点。
- 景点详情抽屉或详情页。
- 视频、全景照片、图片和文字介绍。
- “如何到达”信息。
- 返回地铁层和区域层的入口。

### 5.3.3 景点标记

景点标记至少支持：

- 分类图标。
- 景点主图缩略图。
- 热门 / 推荐状态。
- 与当前站点的关联。
- 聚合标记，避免景点密集区域重叠。
- hover 预览。
- 点击打开详情。

### 5.3.4 景点详情

详情应包含：

- 景点名称。
- 景点主视觉。
- 一句话摘要。
- 详细介绍。
- 历史 / 文化标签。
- 推荐游览时长。
- 开放时间、预约提示、费用等运营字段。
- 最近地铁站。
- 出站方向或步行说明。
- 推荐线路。
- 视频。
- 全景照片或全景查看器。
- 图片画廊。
- 相关景点。

媒体能力建议按以下优先级实现：

1. 首期：图片画廊 + 视频播放。
2. 第二阶段：360 度全景图查看。
3. 后续：视频导览、语音讲解、路线漫游。

### 5.3.5 详情跳转

详情页中的对象关系必须可点击：

- 点击“所属区域”返回第一层并聚焦区域。
- 点击“最近地铁站”返回第二层并聚焦站点。
- 点击“推荐线路”返回第二层并高亮线路。
- 点击“在 2D 总览查看”进入第四层并定位景点。

---

## 5.4 第四层：2D 关系总览层

### 5.4.1 目标

提供最容易理解和最适合规划路线的全局平面视图，统一呈现分区、地铁、站点和景点。

### 5.4.2 页面内容

- 2D 城市底图。
- 分区边界和名称。
- 地铁线路及站点。
- 景点标记。
- 图层开关。
- 线路 / 区域 / 景点筛选。
- 统一搜索。
- 当前选择对象详情。

### 5.4.3 图层控制

用户可以独立开关：

- 分区层。
- 地铁线路层。
- 地铁站点层。
- 景点层。
- 推荐路线层。
- 文字标签层。

### 5.4.4 适用场景

- 快速判断景点和地铁的相对位置。
- 按区域比较文旅资源密度。
- 规划一日或半日的地铁沿线游。
- 从全局地图回到任意线路、站点或景点。

---

## 6. 四层联动机制

### 6.1 统一对象模型

区域、线路、站点和景点均使用唯一 ID：

```text
districtId
lineId
stationId
poiId
```

任意层级选中对象后，其他层级通过关联字段找到对应对象。

### 6.2 进入与返回

| 当前层 | 触发对象 | 目标层 | 目标状态 |
| --- | --- | --- | --- |
| 城市 3D | 区域 | 地铁层 | 聚焦区域，显示相关线路 |
| 城市 3D | 地标 / 景点 | 景点层 | 打开景点详情 |
| 地铁层 | 线路 | 景点层 | 显示该线路沿线景点 |
| 地铁层 | 站点 | 景点层 | 显示该站点周边景点 |
| 景点层 | 所属区域 | 城市 3D | 聚焦区域 |
| 景点层 | 最近地铁站 | 地铁层 | 聚焦站点 |
| 任意层 | 全局总览 | 2D 总览 | 定位当前对象 |
| 2D 总览 | 区域 / 线路 / 景点 | 对应层 | 打开对应对象 |

### 6.3 状态保持

切换层级时至少保持：

- 当前对象 ID。
- 当前筛选条件。
- 当前地图中心。
- 当前缩放级别。
- 当前图层开关。
- 用户是否已打开详情。

### 6.4 动画规范

- 层级切换使用 400-800ms 的过渡动画。
- 3D 到 2D 允许采用旋转、俯视和透明度变化进行视觉转场。
- 不使用过长的连续镜头，避免用户失去方向。
- 动画期间保留对象名称或高亮，帮助用户确认“正在进入哪里”。
- 支持减少动效模式。

---

## 7. MVP 范围

### 7.1 MVP 必须实现

- 四层页面和全局层级导航。
- 第一层 Three.js 3D 分区示意模型。
- 第二层地铁线路、站点、方向和换乘的可视化。
- 第三层景点 2D 标记、详情和图片 / 视频占位能力。
- 第四层 2D 分区、地铁、景点叠加总览。
- 区域、线路、站点、景点之间的跨层跳转。
- 全局搜索。
- 基础筛选和图例。
- 响应式布局，优先支持桌面端，兼容平板。
- 模拟数据和 JSON 数据导入结构。

### 7.2 MVP 暂不实现

- 真实城市级高精度 3D 建模。
- 实时地铁到站、拥挤度和运营状态。
- 在线购票、预约和支付。
- 用户登录、收藏和社交评论。
- 复杂路线规划算法。
- 语音导览。
- 多语言。
- 后台 CMS 完整实现。

### 7.3 迭代方向

**V1.1**

- 360 度全景查看。
- 景点专题路线。
- 收藏和分享。
- 更丰富的站点出入口信息。

**V2.0**

- 接入真实 GIS / 3D 资产。
- 实时地铁数据。
- 多日行程规划。
- 个人化推荐。
- 文旅活动和季节专题。

---

## 8. 技术需求

### 8.1 前端建议

- React 或 Vue 作为应用层框架，具体选择以项目现有环境为准。
- Three.js 负责第一层和第二层的 3D 场景。
- 2D 层可使用 Canvas、SVG 或地图渲染方案。
- 使用统一状态管理保存当前层级和选中对象。
- 通过 Web Worker 或分片加载处理较大的地图和模型数据。

### 8.2 Three.js 场景要求

- 场景、相机、灯光、控制器和对象拾取模块化。
- 区域、线路、站点、地标均使用可复用组件。
- 尽量使用 InstancedMesh、LOD、纹理压缩和按需加载。
- 对象交互采用 Raycaster，并为可交互对象维护业务 ID。
- 所有模型支持显隐、透明度、颜色和高亮状态切换。
- 3D 资产优先支持 GLB / GLTF。
- 渲染层不直接承担内容详情逻辑，通过事件向应用层派发对象选择事件。

### 8.3 路由建议

可以使用单页应用路由，也可以使用 URL 状态：

```text
/overview-3d?district=district-01
/metro?district=district-01&line=line-01&station=station-01
/poi?district=district-01&line=line-01&poi=poi-01
/overview-2d?poi=poi-01
```

这样便于分享、刷新恢复和搜索引擎后续收录。

### 8.4 数据分层

建议拆分为：

- `districts.json`：区域数据和几何数据。
- `metro-lines.json`：线路、站点、换乘关系。
- `pois.json`：景点基础信息和关联关系。
- `media.json`：图片、视频、全景资源。
- `themes.json`：专题路线和推荐内容。

示例：

```json
{
  "poiId": "poi-001",
  "name": "景点名称",
  "districtId": "district-001",
  "stationIds": ["station-001"],
  "lineIds": ["line-001"],
  "category": "历史文化",
  "position": {
    "x": 120,
    "y": 80,
    "z": 0
  },
  "media": {
    "cover": "/media/poi-001-cover.jpg",
    "video": "/media/poi-001.mp4",
    "panorama": "/media/poi-001-pano.jpg"
  }
}
```

---

## 9. 非功能需求

### 9.1 性能

- 首屏可交互时间目标：普通桌面网络环境下不超过 5 秒。
- 首屏优先加载基础区域和必要 UI，3D 细节分层加载。
- 低性能设备提供简化渲染模式。
- 页面切换时避免重复加载已缓存数据。
- 主要交互保持稳定帧率，目标为 45 FPS 以上；高性能设备目标为 60 FPS。

### 9.2 兼容性

- 优先支持桌面端 Chrome、Edge、Safari 最新两个大版本。
- 兼容常见平板横屏。
- 移动端首期支持浏览和详情，不强求完整 3D 操作体验。

### 9.3 可访问性

- 所有关键地图对象必须有可读名称。
- 关键操作不能只依赖颜色区分。
- 提供键盘焦点和基础键盘操作。
- 支持关闭动画或减少动效。
- 视频提供封面、播放控制和替代文本。

### 9.4 数据与内容安全

- 媒体资源使用可配置地址，不在代码中硬编码。
- 景点信息保留来源、更新时间和审核状态。
- 后续接入后台后，支持草稿、审核、发布和下线。
- 第三方视频和全景资源需确认版权和授权范围。

---

## 10. 运营后台需求预留

首期可以只使用 JSON 模拟数据，但数据结构应为后台管理预留：

- 区域管理。
- 地铁线路管理。
- 站点管理。
- 景点管理。
- 媒体资源管理。
- 关联关系管理。
- 推荐专题管理。
- 发布状态管理。
- 数据更新时间和来源记录。

后台不属于 MVP 必须交付内容，但前端不应依赖不可维护的硬编码对象。

---

## 11. 埋点与数据指标

### 11.1 核心指标

- 四层到达率。
- 第一层进入第二层的转化率。
- 地铁站点点击率。
- 景点详情打开率。
- 景点媒体播放率。
- 从景点返回地铁层的比例。
- 2D 总览使用率。
- 单次探索平均浏览层数。
- 单次探索平均停留时间。

### 11.2 关键事件

```text
view_layer
select_district
select_line
select_station
select_poi
open_poi_detail
play_video
open_panorama
switch_to_2d_overview
search
filter_content
```

---

## 12. 验收标准

### 12.1 功能验收

- 用户可以在四层之间自由切换。
- 从区域、线路、站点、景点出发，都能跳转到相关层级。
- 切层后当前对象和上下文保持正确。
- 地铁线路、站点顺序、方向和换乘关系显示正确。
- 景点详情能展示图文，并为视频 / 全景资源提供可用入口。
- 2D 总览可以独立控制各图层。
- 搜索可以定位区域、线路、站点和景点。

### 12.2 视觉与交互验收

- 当前选中对象在所有层级中有明确且一致的高亮。
- 不同线路具有可区分颜色，并配合文字或图例。
- 3D 模型、线路、站点和景点不会大面积重叠到无法点击。
- 层级切换过程中用户能理解当前正在进入的对象。
- 桌面端常见分辨率下无明显文字溢出、遮挡或布局跳动。

### 12.3 工程验收

- 3D 渲染逻辑、业务数据和 UI 逻辑分离。
- 业务对象均有稳定 ID。
- 替换 3D 资产不需要重写层间联动逻辑。
- 没有明显的控制台报错和未处理异常。
- 在低性能设备或不支持 WebGL 时提供降级提示或 2D 版本。

---

## 13. 原型阶段建议排期

| 阶段 | 目标 | 产出 |
| --- | --- | --- |
| 第 1 阶段 | 建立信息架构和视觉方向 | 页面骨架、导航、图例、对象关系 |
| 第 2 阶段 | 完成第一层 | Three.js 城市分区示意模型和区域选择 |
| 第 3 阶段 | 完成第二层 | 地铁线路、站点、方向、换乘和镜头联动 |
| 第 4 阶段 | 完成第三层 | 景点贴图、详情抽屉、图片 / 视频内容 |
| 第 5 阶段 | 完成第四层 | 2D 全局关系图和图层控制 |
| 第 6 阶段 | 打通四层 | 跨层跳转、状态保持、搜索和筛选 |
| 第 7 阶段 | 验收优化 | 性能、响应式、无障碍、错误处理和埋点 |

---

## 14. 主要风险与应对

| 风险 | 影响 | 应对方案 |
| --- | --- | --- |
| 真实 3D 建模周期长 | 延误原型验证 | 首期使用程序化低模和可替换占位资产 |
| 地铁与景点数据不完整 | 影响空间关系可信度 | 先定义统一数据结构，逐步补齐内容 |
| 3D 场景性能不足 | 用户操作卡顿 | 分层加载、LOD、实例化、简化模式 |
| 信息过多导致视觉混乱 | 用户无法理解地图 | 默认聚焦当前对象，其他图层弱化 |
| 3D 操作学习成本高 | 用户不知如何探索 | 保留 2D 总览作为低门槛入口，并提供图例 |
| 视频 / 全景资源版权不明确 | 上线风险 | 记录来源和授权状态，先使用自有或授权素材 |
| 运营信息变化 | 内容过时 | 将动态字段配置化，增加更新时间和审核流程 |

---

## 15. 产品结论

本产品的核心不是单纯展示一张 3D 地图，而是让用户沿着“区域 - 地铁 - 景点 - 全局关系”完成一次有方向的城市探索。四层应该共享同一套对象和数据关系，保证用户从任何入口出发，都能回到其他层级继续理解长春。

第一阶段建议把资源集中在以下三件事：

1. 验证四层联动是否自然。
2. 用 Three.js 完成可替换的 3D 示意模型。
3. 建立区域、地铁、站点、景点和媒体之间的统一数据结构。

真实城市建模、实时地铁数据和高质量全景内容可以在交互模型验证后逐步接入，不应成为原型启动的前置条件。
