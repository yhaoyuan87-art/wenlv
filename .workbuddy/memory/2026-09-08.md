# 2026-09-08

## P0/P1 补全：全局搜索 + WebGL 降级 + 焦点样式（团队流程：寇豆码实现、严过关两轮验收）
- 对照 PRD 体检发现 2 个 MVP 缺口 + 1 个可访问性缺口：全局搜索完全没做（PRD 7.1/12.1）、WebGL 无降级（12.3）、无 :focus-visible（9.3）。
- 新增：SearchBox.jsx（分区/线路/站点/景点分组搜索，Ctrl/Cmd+K 与 / 唤起，Esc stopPropagation 隔离全局快捷键）、WebGLFallback.jsx + three/webgl.js（预检 + try/catch 双保险，?nowebgl=1 强制降级便于测试）、index.css 焦点环与快捷键提示（Breadcrumb 右侧，≤720px 隐藏）。
- QA 第一轮抓到真 Bug：≤900px 视口下降级卡片被 InfoPanel 完全遮挡（按钮埋在面板底下点不到）。修复方案：store 加 webglUnsupported，降级态 InfoPanel 直接 return null（顺带停止 panelInsets 上报）。
- 踩坑记录：agent-browser 守护进程随 bash 调用退出而失活，open+eval 必须串在同一条命令；RAF 动画页只用 eval + 程序化 click；eval 里模拟键盘用 new KeyboardEvent + dispatchEvent。
- QA 第二轮 27/27 全过，lint 0/0，build 成功，已推送。
# 2026-09-08

## P0/P1 补全：全局搜索 + WebGL 降级 + 焦点样式（团队流程：寇豆码实现、严过关两轮验收）
- 对照 PRD 体检发现 2 个 MVP 缺口 + 1 个可访问性缺口：全局搜索完全没做（PRD 7.1/12.1）、WebGL 无降级（12.3）、无 :focus-visible（9.3）。
- 新增：SearchBox.jsx（分区/线路/站点/景点分组搜索，Ctrl/Cmd+K 与 / 唤起，Esc stopPropagation 隔离全局快捷键）、WebGLFallback.jsx + three/webgl.js（预检 + try/catch 双保险，?nowebgl=1 强制降级便于测试）、index.css 焦点环与快捷键提示（Breadcrumb 右侧，≤720px 隐藏）。
- QA 第一轮抓到真 Bug：≤900px 视口下降级卡片被 InfoPanel 完全遮挡（按钮埋在面板底下点不到）。修复方案：store 加 webglUnsupported，降级态 InfoPanel 直接 return null（顺带停止 panelInsets 上报）。
- 踩坑记录：agent-browser 守护进程随 bash 调用退出而失活，open+eval 必须串在同一条命令；RAF 动画页只用 eval + 程序化 click；eval 里模拟键盘用 new KeyboardEvent + dispatchEvent。
- QA 第二轮 27/27 全过，lint 0/0，build 成功，已推送。
