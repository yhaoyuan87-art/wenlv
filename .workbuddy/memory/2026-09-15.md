# 2026-09-15

## 长春地铁文旅：按官方参考图重建全网站点坐标（分支 codex/metro-network-update）

完成：将 `src/data/metro-lines.json` 全量重写为与 `public/media/changchun-network-2026.jpg`（1440×1340 官方图）逐站对齐的数据（151 站/7 线）。

方法（可复用）：
- 当前模型可直接 Read 图片 → 用 PIL 分区裁剪+2x/3x/4x 放大，逐站人工判读底图像素坐标；再用「叠加渲染校验闭环」：把数据点画回底图生成 overlay_full.png，目检偏差并迭代修正（此前 OpenCV 检测/master2 吸附和模板匹配 snap 均不可靠，已弃用）。
- 坐标系：数据为业务坐标，City2DMap 用 `<g transform="translate(40 230) scale(1 1.12)">` 投影到底图。imgX = 40 + bx，imgY = 230 + 1.12*by。3D 用 toXZ(x-500, y-380)。

关键事实（与旧数据差异大，勿回退）：
- 换乘站 20 个：北环城路(1/8)、长春站北(1/4)、长春站(1/3)、解放大路(1/2)、工农广场(1/7)、卫星广场(1/3)、华庆路(1/6)、汽车公园(2/7)、双丰(2/6)、长春西站(2/6)、解放桥(2/3)、吉林大路(2/4)、东环城路(2/7)、伪满皇宫(3/4)、孟家屯(3/7)、职业学院(3/4)、长影世纪城(3/6)、威海路(4/7)、飞跃广场(6/7)、福祉大路(4/6)。
- 6号线与1号线换乘站是「华庆路」不是繁荣路；3号线北端延伸到伪满皇宫、含孟家屯/湖光路/吉林广电，不含长白路/辽宁路/南湖广场/辉南街/硅谷大街/吉林大学；4号线不含世纪广场/吉林广电/亚泰足球基地，含威海路；6号线含飞跃广场/华庆路/亚泰足球基地/聚业大街，不含宽平桥/孟家屯/繁荣路；7号线含西湖大路/南湖新村/南湖公园，不含红旗街/湖光路，且顺序为…威海路→赛得广场→会展大街→中东大市场→岭东路→东环城路；2号线东延含长青/哈尔滨大街/莫俊大街/林溪大街（雾开河大街终点 img(1207,589)）；8号线顺序北环城路→一二三中学→小南→小城子街→…→广通路。
- 站点 ID 按「同名保留旧 ID」原则稳定（pois.json 引用不断）；新站按各线最大序号续号。
- pois.json 已同步修复 5 处失效引用（st-06-09/st-03-11/st-06-05/st-07-17/st-04-11）及对应文案；POI 的 x,y 仍是旧示意坐标（2D 参考图模式下 POI 层透明不可见），如需 3D 精确对齐另开任务。
- 「南湖新村」站名是推断（底图该站无文字标注，按 7 号线规划站名补）。
- 校验：`node scripts/validate-data.mjs` 通过；`vite build` 通过。
- 坐标已全部落地到 metro-lines.json；_crops/（裁剪图/脚本/校验图，88MB）与 public/media/changchun-network-2026.jpg 已于 2026-09-15 晚删除，代码不再引用任何外部底图。

待办：_crops/ 清理与否待俊博确认；POI/行政区坐标精修可选。

## 追加：移除 2D 页面背景交通图
- 俊博要求删掉景点内容页（PoiLayer）和 2D 总览页（MapLayer）的背景地铁图。两处共用 City2DMap.jsx：删除 `<image href="/media/changchun-network-2026.jpg">`，`referenceMap` 置 false，恢复矢量线路/站点/POI/行政区渲染（站点坐标已按官方图校准，矢量走线即真实分布）。svg 去掉 reference-map class。构建通过。
- 图片文件仍保留在 public/media/（未再被 src 引用），如需彻底删文件另说。

## 第三轮：2D 地理对齐 + 3D 管线/标签修复（俊博截图反馈）
- **districts.json 全面重配**（业务坐标系，与校准后的站点同一地理）：yitongRiver 改为沿真实伊通河走向（北湖公园→东大桥→吉林大路东→伊通河站→南溪湿地→东南向）；6 区 polygon/label 按新站群位置重画；landmarks 7 个坐标对齐对应站点，龙嘉机场移到雾开河大街以东 (1260,300)。CityScene(3D城市) 与 City2DMap 共用此数据，一次修改三端生效。
- **pois.json**：23 个 POI 的 x,y 全部重配到对应车站附近（此前是旧示意坐标，与真实地理错位）。
- **MetroScene.js**：TubeGeometry（折线 CurvePath 拐角处 Frenet 框架插值把管体甩离站点柱体）→ 改为逐段 CylinderGeometry + 关节 SphereGeometry，端点严格落在站点坐标；pick() 改用 lineEntries[lid].lineMeshes（原来取 group.children[0]）。
- **index.css**：修复换乘站标签白底白字——.metro-station-label.transfer 的 color: var(--inverse-text)（双主题都是白色）在浅色主题白底上不可见，改为 var(--station-label-text) + 加粗 + 描边阴影；.t-dot 渐变主色改 var(--accent)。
- **City2DMap.jsx**：viewBox 从 0 0 1440 1340 收紧到 VIEW={100,80,1240,1100}（线网实际范围，地图在卡片里更大）；tooltip 百分比定位同步改用 VIEW；伊通河加粗到 12/4.5，标签移到 (662,430)；机场标记移到 (1260,300)。
- 自检：用与 City2DMap 同一投影渲染矢量图目检通过；validate-data 与 vite build 通过（校验脚本 render_vector_map.py 已随 _crops 删除）。
